import 'package:flutter/material.dart';
import '../services/preferences_service.dart';
class OnboardingScreen extends StatelessWidget { const OnboardingScreen({super.key,required this.prefs,required this.onDone}); final PreferencesService prefs; final VoidCallback onDone;
 @override Widget build(BuildContext context)=>Scaffold(body:SafeArea(child:Padding(padding:const EdgeInsets.all(28),child:Column(children:[const Spacer(),ClipRRect(borderRadius:BorderRadius.circular(28),child:Image.asset('assets/images/pastor_abayomi_logo.png',width:180,height:180,fit:BoxFit.cover)),const SizedBox(height:30),Text('Young Minds Bible',textAlign:TextAlign.center,style:Theme.of(context).textTheme.displaySmall?.copyWith(fontWeight:FontWeight.w900)),const SizedBox(height:12),Text('A calm, simple Bible reader built for growing minds.',textAlign:TextAlign.center,style:Theme.of(context).textTheme.titleMedium),const SizedBox(height:30),const _Point(Icons.wifi_off,'Read offline','Keep reading even when there is no internet.'),const _Point(Icons.search,'Search quickly','Find words, passages and references.'),const _Point(Icons.bookmark,'Make it yours','Bookmark, highlight and write private notes.'),const Spacer(),SizedBox(width:double.infinity,child:FilledButton(onPressed:()async{await prefs.setOnboarded();onDone();},child:const Padding(padding:EdgeInsets.all(16),child:Text('Start Reading'))))])))); }
class _Point extends StatelessWidget { const _Point(this.icon,this.title,this.subtitle);final IconData icon;final String title,subtitle;@override Widget build(BuildContext context)=>Padding(padding:const EdgeInsets.symmetric(vertical:8),child:Row(children:[CircleAvatar(child:Icon(icon)),const SizedBox(width:14),Expanded(child:Column(crossAxisAlignment:CrossAxisAlignment.start,children:[Text(title,style:const TextStyle(fontWeight:FontWeight.bold)),Text(subtitle)]))]));}
